@resource.tokens
@resorce.tokens
@resorce.token
Tag.all
Tag.all
Room.find(2).destroy
Room.find(2,3).destroy
Room.find(2).destroy
Room.find(1).destroy
Room.find(3).destroy
Post.destroy_all
Post.all.destroy
Post.destroy_all
